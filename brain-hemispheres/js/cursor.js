/*		window.backgroundParallax = function() {
			
		
		var all = $("*");
	  all.mousemove(function(e){
	var wrapper = $("#wrapper");
    var x = -(e.pageX + this.offsetLeft) / 30;
    var y = -(e.pageY + this.offsetTop) / 30;
    wrapper.css('background-position', x + 'px ' + y + 'px');
  });
  
  $( "#target" ).mousemove(function( event ) {
  var msg = "Handler for .mousemove() called at ",
      msg2 = "Handler for .mousemove() called at ";
  msg += event.pageX + ", " + event.pageY;
  msg2 += event.offsetLeft + ", " + event.offsetTop;
  $( "#log" ).append( "<div>" + msg + " / " + msg2 + "</div>" );
});
    
$( "#other" ).click(function() {
  $( "#target" ).mousemove();
});

			
		};*/